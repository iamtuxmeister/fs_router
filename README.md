# fs_router

Minimal Erlang `gen_statem` call controller for FreeSWITCH via `mod_erlang_event`.

## Requirements

- Erlang/OTP 26+  
- FreeSWITCH with `mod_erlang_event` enabled

## Setup

1. In FreeSWITCH:

